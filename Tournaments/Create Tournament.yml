info:
  name: Create Tournament
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/tournaments
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "id_creator": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
