info:
  name: Add Players to Tournament
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/tournaments/addPlayers
  body:
    type: json
    data: |-
      {
        "id_tournament": 1
        "id_users": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
