info:
  name: Get Tournament Results By Id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/tournaments/:id/results
  params:
    - name: id
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
