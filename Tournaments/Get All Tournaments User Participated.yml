info:
  name: Get All Tournaments User Participated
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/tournaments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
