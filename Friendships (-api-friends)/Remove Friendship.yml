info:
  name: Remove Friendship
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/friends
  body:
    type: json
    data: |-
      {
          "id_friend": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
