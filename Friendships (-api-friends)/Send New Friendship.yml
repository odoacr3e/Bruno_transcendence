info:
  name: Send New Friendship
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/friends
  body:
    type: json
    data: |-
      {
          "id_friend": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
