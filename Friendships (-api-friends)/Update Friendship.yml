info:
  name: Update Friendship
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:8080/api/friends
  body:
    type: json
    data: |-
      {
          "id_friend": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
