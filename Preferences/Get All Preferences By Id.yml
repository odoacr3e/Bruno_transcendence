info:
  name: Get All Preferences By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/preferences/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: text
    data: |-
      {
          "id_friend": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
