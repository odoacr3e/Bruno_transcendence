info:
  name: Get All User Matches
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/matchesHistory
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
