info:
  name: Get User By Username
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/users/username/:username
  params:
    - name: username
      value: Investigatopo12
      type: path
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
