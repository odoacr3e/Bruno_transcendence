info:
  name: Update User Password By Username
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:8080/api/users/updatePassword
  body:
    type: json
    data: |-
      {
          "username": "Investigatopo12",
          "password": "Formaggio123%"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
