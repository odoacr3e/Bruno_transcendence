info:
  name: Update Logged User Password
  type: http
  seq: 9

http:
  method: PATCH
  url: http://localhost:8080/api/users/loggedUpdatePassword
  body:
    type: json
    data: |-
      {
          "password": "Formaggio123%"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
