info:
  name: Get All Users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/users
  params:
    - name: username
      value: k
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
