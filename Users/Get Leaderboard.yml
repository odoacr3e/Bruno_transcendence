info:
  name: Get Leaderboard
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/users/leaderboard
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
