info:
  name: Me
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/users/me
  params:
    - name: username
      value: k
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
