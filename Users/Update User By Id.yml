info:
  name: Update User By Id
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:8080/api/users/
  body:
    type: json
    data: |-
      {
        "name": "Geronimo",
        "surname": "Stilton",
        "username": "Investigatopo12",
        "nationality": "Uganda",
        "preferences": 1
      }
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
