info:
  name: Get User By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/users/:id
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
