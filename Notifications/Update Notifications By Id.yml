info:
  name: Update Notifications By Id
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/api/notifications
  body:
    type: json
    data: |-
      {
          "id_notifications": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
