info:
  name: Get All Pieces
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/pieces
  params:
    - name: name
      value: a
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
