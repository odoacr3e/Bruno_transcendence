info:
  name: Get Piece By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/pieces/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
