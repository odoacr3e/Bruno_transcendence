info:
  name: Verify OTP Before Login
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/auth/verifyOtp
  body:
    type: json
    data: |-
      {
        "id_user": 1,
        "otp": 414862
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
