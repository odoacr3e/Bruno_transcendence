info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/login
  body:
    type: json
    data: |-
      // {
      //     "username": "alfredonia",
      //     "password": "Formaggio12%"
      // }
      {
          "username": "Investigatopo12",
          "password": "Formaggio123%"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
